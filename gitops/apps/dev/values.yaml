# Defaults for helm template / local render.
# Live cluster: root-dev helm.parameters from Terraform
# (repoURL, targetRevision, database.*.host, secrets.*.remoteKey → secrets App).
repoURL: ""
targetRevision: main
project: bank-of-anthos
destinationServer: https://kubernetes.default.svc
destinationNamespace: bank-of-anthos-dev
database:
  accountsDb:
    host: ""
  ledgerDb:
    host: ""
secrets:
  accountsDb:
    remoteKey: ""
  ledgerDb:
    remoteKey: ""
