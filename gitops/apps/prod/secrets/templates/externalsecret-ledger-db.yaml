apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ledger-db-credentials
  labels:
    app.kubernetes.io/name: bank-of-anthos
    app.kubernetes.io/component: ledger-db
spec:
  refreshInterval: {{ .Values.refreshInterval | quote }}
  secretStoreRef:
    name: {{ .Values.clusterSecretStoreName | quote }}
    kind: ClusterSecretStore
  target:
    name: {{ .Values.ledgerDb.secretName | quote }}
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      engineVersion: v2
      mergePolicy: Replace
      data:
        username: "{{ `{{ .username }}` }}"
        password: "{{ `{{ .password }}` }}"
  dataFrom:
    - extract:
        key: {{ required "ledgerDb.remoteKey must be set (Terraform → Argo)" .Values.ledgerDb.remoteKey | quote }}
        conversionStrategy: Default
        decodingStrategy: None
        metadataPolicy: None
