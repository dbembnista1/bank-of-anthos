# Overrides for DEV environment (merged on top of values.yaml by ArgoCD).
# Namespace: bank-of-anthos-dev (shared EKS). Enable via terraform enabled_environments.
#
# database.*.host and global.imageRegistry are injected at sync time by Argo from
# Terraform (root-dev helm.parameters → Application parameters). Do not paste them here.
#
# Image tags: CI (cd-update-values-dev) writes short SHAs after a successful ECR push.

frontend:
  replicas: 1
  image:
    tag: "2db75d6"

userservice:
  replicas: 1
  image:
    tag: "2db75d6"

contacts:
  replicas: 1
  image:
    tag: "2db75d6"

ledgerWriter:
  replicas: 1
  image:
    tag: "2db75d6"

balanceReader:
  replicas: 1
  image:
    tag: "2db75d6"

transactionHistory:
  replicas: 1
  image:
    tag: "2db75d6"

loadgenerator:
  enabled: true
  replicas: 1
  image:
    tag: "2db75d6"

dbInit:
  enabled: true
  loadDemoData: true
